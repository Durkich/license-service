spring:
  application:
    name: licensing-service
  cloud:
    config:
      uri: http://license-configserver:8071
      fail-fast: true
  profiles:
    active: dev
  mvc:
    static-path-pattern: /static/**